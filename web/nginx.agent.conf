server {
    listen 3000;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location = / {
        return 302 /agent/;
    }

    location / {
        try_files $uri $uri/ $uri.html /index.html;
    }
}
